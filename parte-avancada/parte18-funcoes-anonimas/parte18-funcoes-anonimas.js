function somar(a,b){
    let total = a + b;

    return document.write(`A soma de ${a} + ${b} = ${a+b}`);
}
somar(30,10);

let subtrair = (valor1,valor2) => {
    let total = document.write(`<br>A subtração de ${valor1} - ${valor2} = ${valor1-valor2}`);
}

subtrair(20,18);